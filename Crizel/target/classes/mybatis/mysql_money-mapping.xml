<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="usernamespace">
	<select id="monthSum" parameterType="String" resultType="int">
		select sum(price) from money where day like CONCAT('%',#{month},'%') and id=#{id}
	</select>

	<select id="moneyDetail" parameterType="Hashmap" resultType="vo.MoneyVO">
		select * from money where day=#{day} and id=#{id}
	</select>

	<insert id="insert" parameterType="vo.MoneyVO">
		insert into money values(#{item}, #{price}, #{day},(select * from (select ifnull(max(money_id)+1,1) from money)tmp), #{id})
	</insert>

	<delete id="delete" parameterType="Hashmap">
		delete from money where money_id=#{money_id} and id=#{id}
	</delete>

	<select id="login" parameterType="vo.MoneyVO" resultType="vo.MoneyVO">
		select *
		from moneyLogin where id=#{id} and pw=#{pw}
	</select>
	
	<insert id="register" parameterType="vo.MoneyVO">
		insert into moneyLogin
		values(#{id}, #{pw})
	</insert>
	
	<select id="registerCheck" parameterType="String" resultType="String">
		select id from moneyLogin where id=#{re_id}
	</select>
	
	<select id="totalList" parameterType="Hashmap" resultType="vo.MoneyVO">
		select item, sum(price) as price from money where day like CONCAT('%',#{day},'%') and id=#{id} group by item order by price desc
	</select>
</mapper>