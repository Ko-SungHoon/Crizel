<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pentasecurity.isignplus.config.internal.EmailMapper">

    <!-- SMTP 정보를 조회한다. -->
	<select id="findSmtpInfo" resultType="com.pentasecurity.isignplus.config.model.SmtpInfoVO">
		SELECT
			ISENABLE as isEnable,
			SMTPADDR as smtpAddr,
			SMTPPORT as smtpPort,
			EMAILID as emailID,
			EMAILPWD as emailPWD,
			SMTPMETHOD as smtpMethod
	  	FROM
		 	ISIGN_EVENTALARM
	 	LIMIT 1
	</select>
    
</mapper>