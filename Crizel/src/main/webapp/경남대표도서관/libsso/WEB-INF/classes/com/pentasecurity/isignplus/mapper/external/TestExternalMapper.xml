<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pentasecurity.isignplus.test.test.external.TestExternalMapper">

    <select id="selectUser" resultType="com.pentasecurity.isignplus.test.test.TestData">
        SELECT user_id as id, last_nm as name FROM user LIMIT 1
    </select>
    
    <select id="findUserByLoginId" resultType="com.pentasecurity.isignplus.test.test.TestData" parameterType="string">
        select
            id, pwd
        from ISIGN_USER
        where id = #{loginId}
	</select>
	
</mapper>