<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="diary">
	<select id="content" parameterType="String" resultType="com.crizel.diary.DiaryVO">
		SELECT DIARY_ID, DATE_FORMAT(DIARY_DATE,'%Y-%m-%d') DIARY_DATE, CONTENT 
		FROM DIARY 
		WHERE DIARY_DATE = #{day} AND DELETE_STATUS = 'N'
	</select>
	<insert id="diaryInsert" parameterType="com.crizel.diary.DiaryVO">
		INSERT INTO DIARY(DIARY_ID, DIARY_DATE, CONTENT, DELETE_STATUS)
		VALUES((SELECT DIARY_ID FROM(SELECT IFNULL(MAX(DIARY_ID)+1,1) AS DIARY_ID FROM DIARY) DIARY), #{diary_date}, #{content}, 'N')
	</insert>
	<update id="diaryDelete" parameterType="com.crizel.diary.DiaryVO">
		UPDATE DIARY SET DELETE_STATUS = 'D' WHERE DIARY_ID = #{diary_id}
	</update>
	<update id="diaryUpdate" parameterType="com.crizel.diary.DiaryVO">
		UPDATE DIARY SET CONTENT = #{content} WHERE DIARY_ID = #{diary_id}
	</update>
	
	<select id="useDay" parameterType="String" resultType="String">
		SELECT DATE_FORMAT(DIARY_DATE,'%Y-%m-%d') DIARY_DATE FROM DIARY WHERE DIARY_DATE LIKE '%'||#{day}||'%' AND DELETE_STATUS = 'N'
	</select>
	
	
	
</mapper>